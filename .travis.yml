language: java
jdk:
  - oraclejdk8

install:
  - git clone git://github.com/tbodt/cobertura.git
  - cd cobertura
  - mvn -DskipTests install -B
  - cd ..

test: mvn verify -B

after_success:
  - mvn clean cobertura:cobertura coveralls:cobertura -B